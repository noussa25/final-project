<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz App - Test tes connaissances</title>
    <style>
        /* ===========================================
           CSS STYLES - DESIGN ÉLÉGANT ET MODERNE
           =========================================== */
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #ff6b6b 0%, #4ecdc4 50%, #45b7d1 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .quiz-container {
            background:rgba(244, 244, 241, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 600px;
            width: 100%;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .quiz-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1);
        }

        .quiz-header {
            margin-bottom: 30px;
        }

        h1 {
            color: #333;
            font-size: 2.5rem;
            margin-bottom: 10px;
            font-weight: 300;
        }

        .progress-container {
            background: #e0e0e0;
            height: 8px;
            border-radius: 4px;
            margin: 20px 0;
            overflow: hidden;
        }

        .progress-bar {
            height: 100%;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1);
            width: 0%;
            transition: width 0.8s ease;
            border-radius: 4px;
        }

        .question-container {
            margin: 30px 0;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .question-number {
            color: #ff6b6b;
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .question {
            font-size: 1.4rem;
            color: #333;
            margin-bottom: 25px;
            line-height: 1.6;
            font-weight: 500;
        }

        .answers {
            display: grid;
            gap: 15px;
            margin-bottom: 30px;
        }

        .answer {
            background: #f8f9fa;
            border: 2px solid transparent;
            padding: 15px 20px;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.5s ease;
            font-size: 1.1rem;
            text-align: left;
            position: relative;
            overflow: hidden;
        }

        .answer::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: left 0.8s ease;
        }

        .answer:hover {
            border-color: #ff6b6b;
            background: #fff5f5;
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
        }

        .answer:hover::before {
            left: 100%;
        }

        .answer.selected {
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            color: white;
            border-color: #ff6b6b;
        }

        .answer.correct {
            background: #28a745;
            color: white;
            border-color: #28a745;
        }

        .answer.incorrect {
            background: #dc3545;
            color: white;
            border-color: #dc3545;
        }

        .btn {
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 25px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.4s ease;
            font-weight: 600;
            margin: 0 10px;
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.4);
        }

        .btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 10px 30px rgba(255, 107, 107, 0.5);
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .score-container {
            font-size: 1.2rem;
            margin: 20px 0;
        }

        .score {
            color: #ff6b6b;
            font-weight: bold;
            font-size: 2rem;
        }

        .feedback {
            margin: 20px 0;
            padding: 15px;
            border-radius: 10px;
            font-weight: 500;
        }

        .feedback.correct {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .feedback.incorrect {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .results {
            text-align: center;
            padding: 20px;
        }

        .results h2 {
            color: #333;
            font-size: 2rem;
            margin-bottom: 20px;
        }

        .final-score {
            font-size: 3rem;
            color: #ff6b6b;
            font-weight: bold;
            margin: 20px 0;
            text-shadow: 0 2px 4px rgba(255, 107, 107, 0.3);
        }

        .grade {
            font-size: 1.5rem;
            margin: 15px 0;
            padding: 10px 20px;
            border-radius: 25px;
            display: inline-block;
        }

        .grade.excellent {
            background: #d4edda;
            color: #155724;
        }

        .grade.good {
            background: #fff3cd;
            color: #856404;
        }

        .grade.average {
            background: #f8d7da;
            color: #721c24;
        }

        .hidden {
            display: none;
        }

        .review-item {
            background: #f8f9fa;
            margin: 10px 0;
            padding: 15px;
            border-radius: 10px;
            text-align: left;
            border-left: 4px solid #ff6b6b;
        }

        .review-question {
            font-weight: 600;
            margin-bottom: 10px;
        }

        .review-answer {
            margin: 5px 0;
        }

        .review-answer.user {
            color: #dc3545;
        }

        .review-answer.correct {
            color: #28a745;
        }

        /* Styles pour la page d'accueil */
        .home-screen {
            text-align: center;
            padding: 20px;
        }

        .home-title {
            font-size: 3.5rem;
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
            animation: titleGlow 2s ease-in-out infinite alternate;
        }

        @keyframes titleGlow {
            0% { filter: drop-shadow(0 0 20px rgba(255, 107, 107, 0.5)); }
            100% { filter: drop-shadow(0 0 30px rgba(78, 205, 196, 0.7)); }
        }

        .home-description {
            font-size: 1.3rem;
            color: #555;
            margin: 30px 0;
            line-height: 1.8;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .feature {
            background: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 15px;
            border: 2px solid transparent;
            transition: all 0.6s ease;
            backdrop-filter: blur(5px);
        }

        .feature:hover {
            border-color: #ff6b6b;
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(255, 107, 107, 0.2);
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 10px;
            display: block;
        }

        .feature-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 10px;
        }

        .feature-text {
            color: #666;
            font-size: 0.95rem;
        }

        .start-btn {
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            color: white;
            border: none;
            padding: 20px 40px;
            border-radius: 30px;
            font-size: 1.3rem;
            cursor: pointer;
            transition: all 0.5s ease;
            font-weight: 700;
            margin-top: 30px;
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .start-btn:hover {
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 15px 40px rgba(255, 107, 107, 0.6);
        }
            .quiz-container {
                padding: 20px;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            .question {
                font-size: 1.2rem;
            }
            
            .btn {
                padding: 12px 24px;
                font-size: 1rem;
            }
        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }
    </style>
</head>
<body>
    <!-- ========================================
         STRUCTURE HTML - INTERFACE UTILISATEUR
         ======================================== -->
    
    <div class="quiz-container">
        <!-- En-tête du quiz -->
        <div class="quiz-header">
            <h1>🧠 Quiz Master</h1>
            <div class="progress-container">
                <div class="progress-bar" id="progressBar"></div>
            </div>
            <div class="score-container">
                Score: <span class="score" id="currentScore">0</span>
            </div>
        </div>

        <!-- Container principal du quiz -->
        <div id="quizContent">
            <!-- Page d'accueil -->
            <div id="homeScreen" class="home-screen">
                <h1 class="home-title">🧠 Quiz Master Pro</h1>
                <p class="home-description">
                    Bienvenue dans l'expérience de quiz ultime ! 
                    Testez vos connaissances avec notre collection de questions passionnantes.
                </p>
                
                <div class="features">
                    <div class="feature">
                        <span class="feature-icon">🎯</span>
                        <div class="feature-title">10 Questions</div>
                        <div class="feature-text">Défiez-vous avec des questions variées</div>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">⏱️</span>
                        <div class="feature-title">À votre rythme</div>
                        <div class="feature-text">Prenez le temps de réfléchir</div>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">📊</span>
                        <div class="feature-title">Score & Révision</div>
                        <div class="feature-text">Suivez vos progrès en détail</div>
                    </div>
                    <div class="feature">
                        <span class="feature-icon">🔄</span>
                        <div class="feature-title">Rejouable</div>
                        <div class="feature-text">Recommencez autant que vous voulez</div>
                    </div>
                </div>
                
                <button class="start-btn" onclick="goToStart()">Commencer l'Aventure</button>
            </div>

            <!-- Écran de démarrage -->
            <div id="startScreen" class="hidden">
                <h2>🎮 Prêt pour le Challenge ?</h2>
                <p>Ce quiz contient 10 questions sur divers sujets. Montrez ce que vous savez !</p>
                <p style="color: #ff6b6b; font-weight: 600; margin-top: 20px;">
                    ✨ Nouvelle expérience avec des couleurs vibrantes et des animations fluides !
                </p>
                <button class="btn" onclick="startQuiz()">🚀 Lancer le Quiz</button>
                <button class="btn" onclick="goToHome()" style="background: linear-gradient(45deg, #6c757d, #495057); margin-top: 10px;">
                    🏠 Retour Accueil
                </button>
            </div>

            <!-- Écran des questions -->
            <div id="questionScreen" class="hidden">
                <div class="question-container">
                    <div class="question-number" id="questionNumber"></div>
                    <div class="question" id="questionText"></div>
                    <div class="answers" id="answersContainer"></div>
                    <div class="feedback" id="feedback"></div>
                </div>
                <button class="btn" id="nextBtn" onclick="nextQuestion()" style="display: none;">Question Suivante</button>
                <button class="btn" id="submitBtn" onclick="submitAnswer()" style="display: none;">Valider</button>
            </div>

            <!-- Écran des résultats -->
            <div id="resultsScreen" class="hidden">
                <div class="results">
                    <h2>🎉 Quiz Terminé !</h2>
                    <div class="final-score" id="finalScore"></div>
                    <div class="grade" id="gradeText"></div>
                    <p id="encouragement"></p>
                    <button class="btn" onclick="showReview()">📋 Voir les Réponses</button>
                    <button class="btn" onclick="restartQuiz()">🔄 Recommencer</button>
                    <button class="btn" onclick="goToHome()" style="background: linear-gradient(45deg, #6c757d, #495057); margin-top: 15px;">
                        🏠 Retour Accueil
                    </button>
                </div>
            </div>

            <!-- Écran de révision -->
            <div id="reviewScreen" class="hidden">
                <h2>📝 Révision Détaillée</h2>
                <div id="reviewContent"></div>
                <button class="btn" onclick="backToResults()">⬅️ Retour Résultats</button>
                <button class="btn" onclick="restartQuiz()">🆕 Nouveau Quiz</button>
                <button class="btn" onclick="goToHome()" style="background: linear-gradient(45deg, #6c757d, #495057);">
                    🏠 Accueil
                </button>
            </div>
        </div>
    </div>

    <script>
         </script>
</body>
</html>
